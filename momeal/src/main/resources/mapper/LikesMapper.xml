<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.doeat.management.mapper.LikesMapper">

	<!-- 마이페이지 - 식단 좋아요 리스트 출력 -->
	<select id="myLikeList" resultType="map">
		SELECT U.*, L.*, M.*
		FROM
		USERS U
		LEFT JOIN LIKES L ON (U.USER_ID = L.USER_ID)
		LEFT JOIN MEALS M
		ON (L.POST_NO = M.NO)
		WHERE U.USER_ID = 'user3' AND L.TYPE = 'CT04'
	</select>

	<!-- 마이페이지 - 식단 좋아요 상세 -->
	<select id="mylikeSelect" resultType="map">
		SELECT U.*, L.*, M.*
		FROM
		USERS U LEFT JOIN LIKES L ON (U.USER_ID = L.USER_ID)
		LEFT JOIN MEALS M
		ON (L.POST_NO = M.NO)
		WHERE U.USER_ID = 'user3' AND L.POST_NO = #{no}
	</select>
	<!-- 마이페이지 - 챌린지 좋아요 리스트 출력 -->

	<!-- 마이페이지 챌린지 상세 -->

	<!-- 마이페이지 - 좋아요 삭제 -->
	<delete id="myLikeDel"
		parameterType="co.doeat.management.service.LikesVO">
		DELETE FROM LIKES
		WHERE NO = #{no} AND TYPE = #{type}
	</delete>

	<!-- 좋아요 추가 -->

</mapper>